<%= form_for([@course, @course.coursesemesters.build]) do |f| %>
  <% if @coursesemester.errors.any? %>
    <div class="alert alert-danger">
      <h3><%= pluralize(@coursesemester.errors.count, "error") %> prohibited this coursesemester from being saved:</h3>

      <ul>
      <% @coursesemester.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.text_field :name, class: "form-control", placeholder: "Enter Semeter Name" %>
<br>
    <%= f.submit "Submit", class: "btn btn-primary" %>

<% end %>
